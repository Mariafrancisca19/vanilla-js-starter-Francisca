/*const checkBox = document.querySelector("#checkbox");

function listaTareas() {
    let item = document.getElementById("checkbox").value
    let text = document.createTextNode(item)
    let nuevaTrea = document.createElement("input")
    checkboxmaker. type = "checkbox"
    checkboxmaker.name = "name"
    checkboxmaker.value = "value"
    checkboxmaker.id = "id"
    checkboxmaker.classList = "corrected"

    nuevaTrea.appendChild(text);
    nuevaTrea.appendChild(checkboxmaker);
    document.getElementById("lista").appendChild(nuevaTrea);
}

    checkBox.addEventListener("click",listaTareas);

    const itemLista = document.querySelector("elementoLista")

    function marcador() {
        itemLista = document. toggle("donete")
    }

    itemLista.addEventListener("click",marcador)*/

/*METODO GET*/


